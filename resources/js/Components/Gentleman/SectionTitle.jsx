export default function SectionTitle({ children }) {
    return (
        <h2 className="font-serif text-xl font-semibold leading-tight text-[#17110b] sm:text-3xl sm:uppercase">
            {children}
        </h2>
    );
}
